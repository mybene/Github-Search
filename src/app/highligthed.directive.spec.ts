import { HighligthedDirective } from './highligthed.directive';

describe('HighligthedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthedDirective();
    expect(directive).toBeTruthy();
  });
});
